```mermaid
sequenceDiagram
    participant User
    participant Browser
    participant Server

    User->>Browser: Writes note and clicks "Save"
    Browser->>Server: POST /new_note_spa with new note (as JSON)
    activate Server
    Server-->>Browser: 201 Created (or similar success response)
    deactivate Server
    Browser->>Browser: Adds note to UI dynamically (without page reload)

