```mermaid
sequenceDiagram
    participant User
    participant Browser
    participant Server

    User->>Browser: Navigates to /spa
    Browser->>Server: GET /spa
    Server-->>Browser: HTML page
    Browser->>Server: GET /main.css
    Browser->>Server: GET /main.js
    Browser->>Server: GET /data.json (via JavaScript fetch)
    Server-->>Browser: JSON with all notes
